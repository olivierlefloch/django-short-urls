# Textmate project configuration

projectDirectory    = "$CWD"
windowTitle         = "$TM_DISPLAYNAME — PyWork4Core"

# Code formatting settings

softTabs = true
tabSize = 4
softWrap = false
showInvisibles = true

excludeDirectories = "{temp,venv}"
